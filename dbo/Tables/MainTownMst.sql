CREATE TABLE [dbo].[MainTownMst] (
    [MainTownId]   INT           NOT NULL,
    [MainTownName] VARCHAR (100) NOT NULL,
    [AreaId]       INT           NOT NULL,
    PRIMARY KEY CLUSTERED ([MainTownId] ASC)
);

